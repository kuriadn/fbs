[pytest]
DJANGO_SETTINGS_MODULE = fbs_project.settings
python_files = tests.py test_*.py *_tests.py
python_classes = Test*
python_functions = test_*
addopts = 
    --strict-markers
    --strict-config
    --verbose
    --tb=short
    --cov=fbs_app
    --cov-report=html
    --cov-report=term-missing
    --cov-report=xml
    --cov-fail-under=15
    --durations=10
    --reuse-db
    --create-db
markers =
    unit: Unit tests
    integration: Integration tests
    e2e: End-to-end tests
    slow: Slow running tests
    fast: Fast running tests
    performance: Performance tests
    security: Security tests
    database: Database tests
    api: API tests
    ui: User interface tests
    odoo: Odoo integration tests
    msme: MSME feature tests
    accounting: Accounting feature tests
    workflow: Workflow tests
    compliance: Compliance tests
    bi: Business intelligence tests
testpaths = fbs_app/tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning

# Pytest-django specific configuration
django_find_project = false
